group 'SimplifyLearning'

apply plugin: 'java'

java {
    sourceCompatibility = JavaVersion.toVersion(JDKVersion)
    targetCompatibility = JavaVersion.toVersion(JDKVersion)
}

jar {
    archiveBaseName = 'core-algorithms'
    archiveVersion =  '0.9.1'
}
