package com.sl.string.split;

/**
 * Enumeration of popular methods to split a string.
 */
public enum SplitMethodEnum {
    PATTERN_SPLIT,
    STRING_SPLIT,
    STRING_TOKENIZER_SPLIT,
    SCANNER_SPLIT
}
